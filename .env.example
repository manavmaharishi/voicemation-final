# GitHub Token for GPT-4o access via GitHub Models
GITHUB_TOKEN=your_github_token_here

# Optional: Azure AI Inference credentials (if using Azure instead of GitHub Models)
# AZURE_AI_INFERENCE_ENDPOINT=your_azure_endpoint
# AZURE_AI_INFERENCE_KEY=your_azure_key

# Optional: OpenAI API Key (if switching to OpenAI)
# OPENAI_API_KEY=your_openai_api_key

# Optional: Text-to-speech service credentials
# Add any TTS service keys here if needed for voiceover generation